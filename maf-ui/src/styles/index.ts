/**
 * MAF UI — Design System
 *
 * Central export for all design tokens and component style utilities.
 *
 * Usage:
 *   import { buttonStyle, color, spacing, semantic } from '../styles';
 *   <button style={buttonStyle('primary', 'md')}>Save</button>
 *   <div style={{ color: semantic.text, padding: spacing[4] }}>...</div>
 */

// Token definitions
export * from './tokens';

// Component style utilities
export * from './components';
